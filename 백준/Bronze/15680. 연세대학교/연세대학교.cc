#include <iostream>

int main()
{
    int N = -1;
    std::cin >> N;
    if (N == 0)
        std::cout << "YONSEI";
    else
        std::cout << "Leading the Way to the Future";
    return 0;
}